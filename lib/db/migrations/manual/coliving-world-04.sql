-- ============================================================================
-- 合租房世界模型 · 第四批：测试屋标记
--
-- 起因（2026-09-02 事故）：本地测试脚本连的是同一个生产库，我伪造的
-- 入站消息「我上周被裁了」变成了房东的真实对话历史，AI 之后带着这段
-- 编造的前情跟用户说话。**等于凭空给用户安了个人设。**
--
-- 现在本地进程只能写标着 is_test 的房子（见 lib/chat/coliving/guard.ts）。
-- 真人住的那栋，本地怎么都写不进去。
-- ============================================================================

alter table coliving.household
  add column if not exists is_test boolean not null default false;

-- 加入码那列已经废弃（改成从房东单点切入），留着不用但也不删——
-- 删列是不可逆操作，而它不占什么。
comment on column coliving.household.join_code is
  '已废弃：2026-09-01 起改为从房东号码单点切入，不再用加入码';
